
        // could improve the code by preventing someone with the same username from signing in.  we've done something similar to this in previous assessments.

    // Message.create(schema, callback function for the .save)  this creates the instance and saves it at the same time